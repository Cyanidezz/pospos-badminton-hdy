'use client';
import {useEffect,useState} from 'react';
import {useParams} from 'next/navigation';
export default function Track(){const {token}=useParams(),[job,setJob]=useState<any>(null),[error,setError]=useState('');useEffect(()=>{const load=()=>fetch('/api/track/'+token).then(r=>r.json()).then((j:any)=>j.error?setError(j.error):setJob(j)).catch(()=>setError('โหลดไม่สำเร็จ กรุณาลองอีกครั้ง'));load();const timer=setInterval(load,30000);return()=>clearInterval(timer);},[token]);return <main className="tracking"><div className="eyebrow">BADMINTON · STRINGING SERVICE</div><h1>ติดตามสถานะไม้</h1>{error?<p>{error}</p>:!job?<p>กำลังโหลด…</p>:<><p>ใบรับไม้ #{job.id.slice(0,8).toUpperCase()}</p><h2>{job.racket}</h2><div className="track-status">{job.status}</div><ol>{['รับไม้','รอขึ้นเอ็น','กำลังขึ้นเอ็น','พร้อมรับไม้','คืนไม้แล้ว'].map((s,i)=><li className={i<=['รับไม้','รอขึ้นเอ็น','กำลังขึ้นเอ็น','พร้อมรับไม้','คืนไม้แล้ว'].indexOf(job.status)?'done':''} key={s}>{s}</li>)}</ol><p>{job.paid?'ชำระเงินแล้ว':`ชำระวันรับไม้ ${(job.amount/100).toLocaleString('th-TH')} บาท`}</p>{job.lineOa&&<p><a className="text-button" href={'https://line.me/R/oaMessage/'+encodeURIComponent(job.lineOa)+'/?'+encodeURIComponent('LINK '+token)} target="_blank" rel="noreferrer">รับการแจ้งเตือนผ่าน LINE ↗</a><br/><small>เพิ่มเพื่อนและกดส่งรหัสที่แสดงในแชทของร้าน</small></p>}<small>อัปเดตอัตโนมัติทุก 30 วินาที · เก็บลิงก์นี้ไว้เป็นส่วนตัว</small></>}</main>}
