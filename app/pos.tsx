'use client';
import {ProductImport} from './product-import';
import {useEffect,useState} from 'react';
import {ShoppingBag,Package,Grid2X2,Scissors,CalendarDays,Users,Settings,ChartNoAxesCombined,Search,ScanBarcode,Plus,ArrowUpRight,ArrowRight,Trash2,Check,RefreshCw,ReceiptText,Wallet,ChevronRight,Download} from 'lucide-react';
import {Sidebar,SidebarProvider,SidebarContent,SidebarHeader,SidebarFooter,SidebarMenu,SidebarMenuItem,SidebarMenuButton,SidebarTrigger} from '@/components/ui/sidebar';
import {Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription} from '@/components/ui/dialog';
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';
import {Switch} from '@/components/ui/switch';
import {Table,TableHeader,TableBody,TableRow,TableHead,TableCell} from '@/components/ui/table';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
import {Toaster,toast} from 'sonner';
import QRCode from 'qrcode';
import {AlertDialog,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction} from '@/components/ui/alert-dialog';
import BarcodeScanner from './barcode-scanner';
const defaultCats=['ทั้งหมด','ไม้แบดมินตัน','เอ็นแบดมินตัน','รองเท้า','เสื้อผ้า','กระเป๋า','อุปกรณ์เสริม'];
const statuses=['รับไม้','รอขึ้นเอ็น','กำลังขึ้นเอ็น','พร้อมรับไม้','คืนไม้แล้ว'];
const fmt=(v:number)=>((v||0)/100).toLocaleString('th-TH',{minimumFractionDigits:2,maximumFractionDigits:2});
const day=(s:string)=>s?new Date(s).toLocaleDateString('en-CA',{timeZone:'Asia/Bangkok'}):'';
const today=()=>day(new Date().toISOString());
function Choice({value,onChange,options}:any){return <Select value={value} onValueChange={onChange}><SelectTrigger className="choice"><SelectValue placeholder="เลือก…"/></SelectTrigger><SelectContent>{options.map((o:any)=><SelectItem key={typeof o==='string'?o:o.id} value={typeof o==='string'?o:o.id}>{typeof o==='string'?o:o.name}</SelectItem>)}</SelectContent></Select>}
function Field({label,children}:any){return <label className="field"><span>{label}</span>{children}</label>}
function Empty({text,action}:any){return <div className="empty"><Package size={32}/><h3>{text}</h3>{action}</div>}
export default function POS(){const [data,setData]=useState<any>(null),[error,setError]=useState(''),[page,setPage]=useState('pos'),[search,setSearch]=useState(''),[category,setCategory]=useState('ทั้งหมด'),[cart,setCart]=useState<any[]>([]),[discount,setDiscount]=useState('0'),[discountType,setDiscountType]=useState('baht'),[method,setMethod]=useState('เงินสด'),[modal,setModal]=useState(''),[form,setForm]=useState<any>({}),[busy,setBusy]=useState(false),[uploading,setUploading]=useState(false),[selected,setSelected]=useState<any>(null),[qr,setQr]=useState(''),[range,setRange]=useState('day'),[date,setDate]=useState(today()),[jobFilter,setJobFilter]=useState('ทั้งหมด'),[requestId,setRequestId]=useState(''),[scanTarget,setScanTarget]=useState(''),[saleScreen,setSaleScreen]=useState('catalog'),[productView,setProductView]=useState('cards'),[columns,setColumns]=useState('2'),[inventoryTab,setInventoryTab]=useState('products'),[archiveTarget,setArchiveTarget]=useState<any>(null),[deleteTarget,setDeleteTarget]=useState<any>(null),[deleteName,setDeleteName]=useState(''),[receiveForm,setReceiveForm]=useState<any>({productId:'',qty:1,query:''});
async function load(){try{const r=await fetch('/api/data',{cache:'no-store'}),d:any=await r.json();if(!r.ok)throw new Error(d.error);setData(d);setError('');}catch(e:any){setError(e.message);}}
useEffect(()=>{load();try{const prefs=JSON.parse(localStorage.getItem('pos-display')||'{}');if(['cards','list'].includes(prefs.view))setProductView(prefs.view);if(['2','3'].includes(prefs.columns))setColumns(prefs.columns)}catch{}},[]);
function displayPrefs(view:string,count:string){setProductView(view);setColumns(count);try{localStorage.setItem('pos-display',JSON.stringify({view,columns:count}))}catch{}}
useEffect(()=>{if(selected&&data){setSelected(data.jobs.find((j:any)=>j.id===selected.id)||selected);}},[data]);
const [stockFilter,setStockFilter]=useState('ทั้งหมด');const [inventoryKind,setInventoryKind]=useState('other');
function open(kind:string,initial:any={}){setForm(kind==='product'?{qty:1,...initial}:kind==='expense'?{...initial,items:[{name:'',category:'',amount:''}]}:initial);setModal(kind);setRequestId(crypto.randomUUID());}
async function act(action:string,body:any={},close=true){if(busy||uploading)return null;if(!navigator.onLine){toast.error('กรุณาเชื่อมต่ออินเทอร์เน็ตก่อนบันทึก');return null;}setBusy(true);try{const r=await fetch('/api/data',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,requestId:requestId||crypto.randomUUID(),...body})}),d:any=await r.json();if(!r.ok)throw new Error(d.error);toast.success(d.notification||(d.barcode?'เพิ่มสินค้าแล้ว · '+d.barcode:'บันทึกเรียบร้อย'));if(close)setModal('');setRequestId(crypto.randomUUID());await load();return d;}catch(e:any){toast.error(e.message);return null;}finally{setBusy(false);}}
async function upload(files:FileList|null,key:string){if(!files)return;setUploading(true);try{const ids:string[]=[];for(const file of Array.from(files)){const fd=new FormData();fd.append('file',file);const r=await fetch('/api/upload',{method:'POST',body:fd}),d:any=await r.json();if(!r.ok)throw new Error(d.error);ids.push(d.id);}setForm((f:any)=>({...f,[key]:key==='photos'?[...(f.photos||[]),...ids]:ids[0]}));toast.success('แนบรูปแล้ว');}catch(e:any){toast.error(e.message);}finally{setUploading(false);}}
function add(p:any){setCart(c=>c.some(x=>x.id===p.id)?c.map(x=>x.id===p.id?{...x,qty:x.qty+1}:x):[...c,{...p,qty:1,salePrice:p.price/100,note:''}]);}
function newScannedProduct(code:string){code=code.trim();if(!code||busy||modal)return;const archived=allProducts.find((p:any)=>p.active===0&&(p.barcode===code||p.scan_code===code));if(archived){toast.error('สินค้านี้อยู่ในสินค้าที่ลบ ให้เจ้าของร้านกู้คืนก่อนขาย');return;}setSearch('');open('quickProduct',{barcode:code,name:'',price:'',category:'',unit:'ชิ้น',qty:1});}
function detected(code:string){code=code.trim();const target=scanTarget;setScanTarget('');const p=products.find((p:any)=>(p.barcode===code||p.scan_code===code));if(target==='pos'){if(p){add(p);setSearch('');toast.success('เพิ่ม '+p.name+' ลงตะกร้าแล้ว')}else{newScannedProduct(code)}}else if(target==='stockReceive'){setReceiveForm((f:any)=>({...f,query:code,productId:p?.id||''}));if(!p)toast.info('ยังไม่พบสินค้ารหัสนี้')}else if(target==='receive'){setForm((f:any)=>({...f,query:code,productId:p?.id||''}));if(!p)toast.info('ยังไม่พบสินค้า กรุณาเพิ่มสินค้าใหม่ก่อนรับเข้า')}else if(target==='product')setForm((f:any)=>({...f,barcode:code}));}
async function uploadExpense(files:FileList|null,index:number){if(!files||uploading)return;const existing=form.items[index].photos||[];if(existing.length+files.length>8){toast.error('แนบได้ไม่เกิน 8 รูปต่อรายการ');return;}setUploading(true);try{for(const file of Array.from(files)){const fd=new FormData();fd.append('file',file);const r=await fetch('/api/upload',{method:'POST',body:fd}),d:any=await r.json();if(!r.ok)throw new Error(d.error);setForm((f:any)=>({...f,items:f.items.map((x:any,i:number)=>i===index?{...x,photos:[...(x.photos||[]),d.id]}:x)}))}toast.success('แนบรูปแล้ว')}catch(e:any){toast.error(e.message)}finally{setUploading(false)}}
function showJob(j:any){setSelected(j);setModal('jobDetail');setRequestId(crypto.randomUUID());QRCode.toDataURL(window.location.origin+'/track/'+j.token,{width:220,margin:2}).then(setQr);}
const cats=data?.categories?['ทั้งหมด',...data.categories]:defaultCats;
const owner=data?.me.role==='owner',allProducts=data?.products||[],products=allProducts.filter((p:any)=>p.active!==0),members=data?.members||[],jobs=data?.jobs||[],filtered=products.filter((p:any)=>(category==='ทั้งหมด'||p.category===category)&&`${p.name} ${p.barcode} ${p.scan_code||''}`.toLowerCase().includes(search.toLowerCase())),gross=cart.reduce((s,x)=>s+x.qty*Math.round(Number(x.salePrice)*100),0),lineDiscounts=cart.reduce((s,x)=>s+(x.discountType==='percent'?Math.round(x.qty*Math.round(Number(x.salePrice)*100)*Number(x.discount||0)/100):Math.round(Number(x.discount||0)*100)),0),afterLines=gross-lineDiscounts,disc=discountType==='percent'?Math.round(afterLines*Number(discount)/100):Math.round(Number(discount)*100),total=Math.max(0,afterLines-disc);
const inRange=(s:string)=>{const d=day(s);return range==='day'?d===date:range==='month'?d.slice(0,7)===date.slice(0,7):d.slice(0,4)===date.slice(0,4);};
const sales=(data?.sales||[]).filter((s:any)=>inRange(s.created)),saleIds=new Set(sales.map((s:any)=>s.id)),items=(data?.items||[]).filter((x:any)=>saleIds.has(x.sale_id)),revenue=sales.reduce((s:number,x:any)=>s+x.total,0),missing=items.filter((x:any)=>x.cost===null),cost=items.reduce((s:number,x:any)=>s+(x.cost||0)*x.qty,0),commission=items.reduce((s:number,x:any)=>s+(x.category==='เอ็นแบดมินตัน'?0:Math.round(x.net*.02)),0),labor=jobs.filter((j:any)=>j.completed&&inRange(j.completed)).length*1500,expenses=(data?.expenses||[]).filter((x:any)=>inRange(x.date+'T05:00:00Z')).reduce((s:number,x:any)=>s+x.amount,0);
const nav=[['pos','ขายหน้าร้าน',ShoppingBag],['inventory','คลังสินค้า',Package],['stringing','งานขึ้นเอ็น',Scissors],...(owner?[['dashboard','ภาพรวมร้าน',ChartNoAxesCombined],['expenses','บันทึกค่าใช้จ่าย',ReceiptText]]:[]),['leave','วันลาพนักงาน',CalendarDays],...(owner||data?.config.show_earnings?[['earnings','ค่าแรง / คอมมิชชั่น',Wallet]]:[]),...(owner?[['team','จัดการผู้ใช้และสิทธิ์',Users],['settings','ตั้งค่าร้าน',Settings]]:[])];
const name=(id:string)=>members.find((m:any)=>m.id===id)?.name||'—';
const dateControl=<div className="date-controls"><Tabs value={range} onValueChange={setRange}><TabsList>{[['day','วัน'],['month','เดือน'],['year','ปี']].map(([v,t])=><TabsTrigger value={v} key={v}>{t}</TabsTrigger>)}</TabsList></Tabs><input aria-label="วันที่รายงาน" type="date" value={date} onChange={e=>setDate(e.target.value)}/></div>;
if(!data)return <div className="startup"><img className="brand-logo" src="/wingpro-logo.jpeg" alt="Wingpro"/><h1>Wingpro POS</h1><p>{error||'กำลังเปิดร้าน…'}</p>{error&&<><button onClick={load}>ลองอีกครั้ง</button><a href="/signin-with-chatgpt?return_to=/" target="_top">เข้าสู่ระบบด้วย ChatGPT</a></>}</div>;
return <SidebarProvider><Toaster richColors position="top-center"/><Sidebar className="store-sidebar"><SidebarHeader><div className="brand"><img className="brand-logo" src="/wingpro-logo.jpeg" alt="Wingpro"/><div><strong>WINGPRO</strong><small>STORE MANAGEMENT</small></div></div></SidebarHeader><SidebarContent><div className="nav-label">พื้นที่ทำงาน</div><SidebarMenu>{nav.map(([id,label,Icon]:any)=><SidebarMenuItem key={id}><SidebarMenuButton isActive={page===id} onClick={()=>{setPage(id);setSearch('');setCategory('ทั้งหมด');}}><Icon size={19}/><span>{label}</span>{id==='inventory'&&products.some((p:any)=>p.stock-p.reserved<=0||p.stock-p.reserved<p.low_stock)&&<b className="nav-count">{products.filter((p:any)=>p.stock-p.reserved<=0||p.stock-p.reserved<p.low_stock).length}</b>}{id==='stringing'&&<b className="nav-count">{jobs.filter((j:any)=>!j.returned).length}</b>}</SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></SidebarContent><SidebarFooter><div className="store-user"><div className="avatar">{data.me.name.slice(0,1)}</div><div><strong>{data.me.name}</strong><small>{owner?'เจ้าของกิจการ':'พนักงานแคชเชียร์'}</small></div></div><a className="signout" href="/signout-with-chatgpt?return_to=/" target="_top">ออกจากระบบ</a></SidebarFooter></Sidebar><div className="workspace"><header className="topbar"><div className="flex items-center gap-3"><SidebarTrigger/><span>{data.config.shop}</span><span className="top-divider">/</span><strong>{nav.find(x=>x[0]===page)?.[1] as string}</strong></div><div className="top-right"><span>{new Date().toLocaleDateString('th-TH',{dateStyle:'medium'})}</span><button className="icon-button" title="รีเฟรช" onClick={load}><RefreshCw size={17}/></button></div></header>{error&&<div className="notice">{error} <button onClick={load}>โหลดใหม่</button></div>}
<main className={'main '+(page==='pos'?'pos-main register-main':'')}>
<div className="page-heading"><div><div className="eyebrow">{page==='pos'?'POINT OF SALE':page==='stringing'?'STRINGING SERVICE':'WINGPRO STORE'}</div><h1>{nav.find(x=>x[0]===page)?.[1] as string}</h1><p>{page==='pos'?'ค้นหาสินค้า เพิ่มลงตะกร้า แล้วรับชำระเงิน':page==='stringing'?'ดูแลทุกไม้ ตั้งแต่รับเข้า จนถึงมือลูกค้า':page==='inventory'?'สต๊อกพร้อมขาย และเอ็นที่อยู่ระหว่างขึ้น':page==='dashboard'?'ยอดขายและผลประกอบการตามวันที่รับชำระ':page==='earnings'?'ค่าแรง 15 บาท/ไม้ · คอมมิชชั่นสินค้า 2% ไม่รวมเอ็น':''}</p></div>{page==='inventory'?<div className="actions">{owner&&<button className="secondary" onClick={()=>setInventoryTab('categories')}>หมวดหมู่</button>}<button onClick={()=>open('product',{category:'ไม้แบดมินตัน',unit:'ชิ้น',price:''})}><Plus size={17}/>เพิ่มสินค้า</button></div>:page==='stringing'?<button onClick={()=>open('job',{productId:'',stringerId:data.me.id,photos:[],amount:'',condition:'ไม่มีตำหนิที่พบ',tension:'',note:''})}><Plus size={18}/>รับไม้ใหม่</button>:page==='expenses'?<button onClick={()=>open('expense',{date:today()})}><Plus size={18}/>บันทึกค่าใช้จ่าย</button>:page==='leave'?<button onClick={()=>open('leave',{type:'ลากิจ',start:today(),end:today()})}><Plus size={18}/>ขอลา</button>:page==='team'?<button onClick={()=>open('member',{role:'cashier'})}><Plus size={18}/>เพิ่มผู้ใช้</button>:null}</div>
{page==='pos'&&<><div className="register-toolbar"><Tabs value={saleScreen} onValueChange={setSaleScreen}><TabsList><TabsTrigger value="catalog">เลือกสินค้า</TabsTrigger><TabsTrigger value="cart">ตะกร้า ({cart.reduce((s,x)=>s+x.qty,0)})</TabsTrigger></TabsList></Tabs><button className="secondary" onClick={()=>setScanTarget('pos')}><ScanBarcode size={19}/>สแกน</button></div><div className={'pos-layout register-layout screen-'+saleScreen}><section className="catalog"><div className="search-box"><Search size={20}/><input autoFocus placeholder="ค้นหาชื่อสินค้า หรือสแกนบาร์โค้ด…" value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();const p=products.find((p:any)=>(p.barcode===search||p.scan_code===search));if(p){add(p);setSearch('');}else if(filtered.length===1){add(filtered[0]);setSearch('');}else if(search.trim()&&!products.some((p:any)=>`${p.name} ${p.barcode} ${p.scan_code||''}`.toLowerCase().includes(search.trim().toLowerCase())))newScannedProduct(search);else toast.info('เลือกสินค้าจากรายการค้นหา');}}}/><button type="button" className="icon-button scan-button" aria-label="สแกนบาร์โค้ดด้วยกล้อง" onClick={()=>setScanTarget('pos')}><ScanBarcode size={22}/></button></div><div className="category-tabs"><Tabs value={category} onValueChange={setCategory}><TabsList>{cats.map(c=><TabsTrigger value={c} key={c}>{c}</TabsTrigger>)}</TabsList></Tabs></div><div className="catalog-display"><span>{filtered.length} รายการ</span><Tabs value={productView} onValueChange={v=>displayPrefs(v,columns)}><TabsList><TabsTrigger value="cards">การ์ด</TabsTrigger><TabsTrigger value="list">รายการ</TabsTrigger></TabsList></Tabs>{productView==='cards'&&<Choice value={columns} onChange={(v:string)=>displayPrefs(productView,v)} options={[{id:'2',name:'2 คอลัมน์'},{id:'3',name:'3 คอลัมน์'}]}/>}</div>{!filtered.length?<Empty text={products.length?'ไม่พบสินค้าที่ค้นหา':'เริ่มต้นด้วยการเพิ่มสินค้าของร้าน'} action={<button onClick={()=>open('product',{category:'ไม้แบดมินตัน',unit:'ชิ้น',price:''})}><Plus size={18}/>เพิ่มสินค้า</button>}/>:<div className={'product-grid register-products view-'+productView+' columns-'+columns}>{filtered.map((p:any)=>{const qty=cart.find(x=>x.id===p.id)?.qty||0;return <button className={'product-card register-product '+(qty?'is-selected':'')} key={p.id} onClick={()=>add(p)}><div className="register-product-name">{p.image&&<img className="register-product-image" src={'/api/files/'+p.image} alt={p.name} loading="lazy"/>}<span className={'product-availability '+(p.stock-p.reserved<=0?'unavailable':'')}>{p.stock-p.reserved<=0?'หมด · ขายได้':'เหลือ '+(p.stock-p.reserved)}</span>{qty>0&&<span className="selected-qty">{qty}</span>}<h3>{p.name}</h3></div><div className="register-product-price"><strong>฿{fmt(p.price)}</strong><Plus size={16}/></div></button>})}</div>}</section><aside id="sale-cart" className="cart"><div className="cart-heading"><div><ReceiptText size={20}/><h2>รายการขาย</h2><span className="count">{cart.reduce((s,x)=>s+x.qty,0)}</span></div><button className="icon-button" disabled={!cart.length} title="ล้างตะกร้า" onClick={()=>setCart([])}><Trash2 size={17}/></button></div><div className="cart-items">{!cart.length?<div className="cart-empty"><ShoppingBag size={38}/><h3>ตะกร้ายังว่าง</h3><p>เลือกสินค้าหรือสแกนบาร์โค้ด<br/>เพื่อเริ่มรายการขาย</p></div>:cart.map((x,i)=><div className="cart-item" key={x.id}><div className="cart-item-title"><b>{x.name}</b><button className="icon-button" aria-label="ลบสินค้า" onClick={()=>setCart(c=>c.filter(y=>y.id!==x.id))}><Trash2 size={14}/></button></div><div className="cart-item-edit"><button type="button" className="secondary quantity-button" aria-label="ลดจำนวน" disabled={x.qty<=1} onClick={()=>setCart(c=>c.map((y,k)=>k===i?{...y,qty:Math.max(1,y.qty-1)}:y))}>−</button><input aria-label="จำนวน" type="number" min="1" value={x.qty} onChange={e=>setCart(c=>c.map((y,k)=>k===i?{...y,qty:Number(e.target.value)}:y))}/><button type="button" className="secondary quantity-button" aria-label="เพิ่มจำนวน" onClick={()=>setCart(c=>c.map((y,k)=>k===i?{...y,qty:y.qty+1}:y))}>+</button><span>×</span><input aria-label="ราคาขาย" type="number" min="0" step="0.01" value={x.salePrice} onChange={e=>setCart(c=>c.map((y,k)=>k===i?{...y,salePrice:e.target.value}:y))}/><strong>{fmt(x.qty*Number(x.salePrice)*100)}</strong></div>{Number(x.salePrice)*100!==x.price&&<input className="price-note" placeholder="หมายเหตุที่แก้ไขราคา (จำเป็น)" value={x.note} onChange={e=>setCart(c=>c.map((y,k)=>k===i?{...y,note:e.target.value}:y))}/>}<div className="line-discount"><span>ส่วนลดรายการ</span><input aria-label={'ส่วนลด '+x.name} type="number" inputMode="decimal" min="0" step="0.01" value={x.discount||''} placeholder="0" onChange={e=>setCart(c=>c.map((y,k)=>k===i?{...y,discount:e.target.value}:y))}/><Choice value={x.discountType||'baht'} onChange={(v:string)=>setCart(c=>c.map((y,k)=>k===i?{...y,discountType:v}:y))} options={[{id:'baht',name:'บาท'},{id:'percent',name:'%'}]}/></div><p className="line-discount-note">ส่วนลดบาทคิดต่อรายการ รวมทุกชิ้น</p><div className="line-net">หลังลดรายการ <b>฿{fmt(Math.max(0,x.qty*Math.round(Number(x.salePrice)*100)-(x.discountType==='percent'?Math.round(x.qty*Math.round(Number(x.salePrice)*100)*Number(x.discount||0)/100):Math.round(Number(x.discount||0)*100))))}</b></div></div>)}</div><div className="cart-bottom"><div className="summary-row"><span>ยอดสินค้า</span><b>฿{fmt(gross)}</b></div><div className="summary-row"><span>ส่วนลดรายสินค้า</span><b>฿{fmt(lineDiscounts)}</b></div><div className="discount-row"><span>ส่วนลดท้ายบิล</span><input aria-label="ส่วนลด" type="number" min="0" value={discount} onChange={e=>setDiscount(e.target.value)}/><Choice value={discountType} onChange={setDiscountType} options={[{id:'baht',name:'บาท'},{id:'percent',name:'%'}]}/></div><div className="grand-total"><span>ยอดชำระ</span><strong>฿{fmt(total)}</strong></div><button className="checkout" disabled={!cart.length} onClick={()=>open('checkout',{})}>รับชำระเงิน<ArrowRight size={20}/></button><p className="cart-note">ขายได้แม้สต๊อกไม่พอ · ระบบบันทึกยอดติดลบ</p></div></aside></div><div className="register-bottom"><div><small>พนักงาน · {data.me.name}</small><span>{cart.length} รายการ · {cart.reduce((s,x)=>s+x.qty,0)} ชิ้น</span></div>{saleScreen==='cart'&&<button className="secondary" onClick={()=>setSaleScreen('catalog')}>เพิ่มสินค้า</button>}<button className="register-total" disabled={saleScreen==='cart'&&!cart.length} onClick={()=>saleScreen==='catalog'?setSaleScreen('cart'):open('checkout',{})}><strong>฿{fmt(total)}</strong><span>{saleScreen==='catalog'?'ดูตะกร้า':'ชำระเงิน'}<ArrowRight size={18}/></span></button></div></>}
{page==='inventory'&&<><Tabs value={inventoryTab} onValueChange={setInventoryTab}><TabsList className="warehouse-tabs"><TabsTrigger value="products">รายการสินค้า</TabsTrigger><TabsTrigger value="receive">รับสินค้า</TabsTrigger><TabsTrigger value="history">ประวัติสต๊อก</TabsTrigger>{owner&&<><TabsTrigger value="categories">หมวดหมู่</TabsTrigger><TabsTrigger value="import">นำเข้า Excel</TabsTrigger></>}{owner&&<TabsTrigger value="archived">สินค้าที่ลบ</TabsTrigger>}</TabsList></Tabs>
{owner&&inventoryTab==='import'&&<ProductImport reload={load}/>} {owner&&inventoryTab==='categories'&&<section className="panel warehouse-receive"><button onClick={()=>open('category',{name:''})}>เพิ่มหมวดหมู่</button>{cats.slice(1).map(c=><div className="category-manage" key={c}><span>{c}<small> {allProducts.filter((p:any)=>p.category===c).length} สินค้า</small></span><button className="secondary" disabled={c==='เอ็นแบดมินตัน'} onClick={()=>open('renameCategory',{name:c,newName:c})}>แก้ชื่อ</button><button className="secondary" disabled={c==='เอ็นแบดมินตัน'} onClick={()=>open('deleteCategory',{name:c})}>ลบ</button></div>)}</section>}
{(inventoryTab==='products'||inventoryTab==='archived')&&<><Tabs value={inventoryKind} onValueChange={v=>{setInventoryKind(v);setCategory('ทั้งหมด');setSearch('');}}><TabsList className="warehouse-tabs"><TabsTrigger value="other">สินค้าอื่น</TabsTrigger><TabsTrigger value="strings">เอ็นแบดมินตัน</TabsTrigger></TabsList></Tabs><div className="stock-filters">{['ทั้งหมด','พร้อมขาย','ใกล้หมด','หมดแล้ว'].map(t=><button key={t} className={stockFilter===t?'':'secondary'} onClick={()=>setStockFilter(t)}>{t}</button>)}</div><p className="stock-alert">ใกล้หมด {products.filter((p:any)=>((p.category==='เอ็นแบดมินตัน')===(inventoryKind==='strings'))&&p.stock-p.reserved>0&&p.stock-p.reserved<p.low_stock).length} รายการ · หมดแล้ว {products.filter((p:any)=>((p.category==='เอ็นแบดมินตัน')===(inventoryKind==='strings'))&&p.stock-p.reserved<=0).length} รายการ (คิดจากยอดพร้อมขาย)</p><div className="panel-tools"><div className="search-box"><Search size={18}/><input placeholder="ค้นหาชื่อ / รหัสสินค้า" value={search} onChange={e=>setSearch(e.target.value)}/></div><Choice value={category} onChange={setCategory} options={cats}/></div><div className="warehouse-grid">{allProducts.filter((p:any)=>(inventoryTab==='archived'?p.active===0:p.active!==0)&&((p.category==='เอ็นแบดมินตัน')===(inventoryKind==='strings'))&&(stockFilter==='ทั้งหมด'||(stockFilter==='หมดแล้ว'?p.stock-p.reserved<=0:stockFilter==='ใกล้หมด'?p.stock-p.reserved>0&&p.stock-p.reserved<p.low_stock:p.stock-p.reserved>0))&&(category==='ทั้งหมด'||p.category===category)&&`${p.name} ${p.barcode} ${p.scan_code||''}`.toLowerCase().includes(search.toLowerCase())).map((p:any)=><article className="warehouse-card" key={p.id}><header>{p.image&&<img src={'/api/files/'+p.image} alt=""/>}<div><h3>{p.name}</h3><small>{p.barcode} · {p.category}</small></div><strong>฿{fmt(p.price)}</strong></header><span className={"badge stock-state "+(p.stock-p.reserved<=0?"unpaid":p.stock-p.reserved<p.low_stock?"stock-low":"paid")}>{p.stock-p.reserved<=0?"หมดแล้ว":p.stock-p.reserved<p.low_stock?"ใกล้หมด":"พร้อมขาย"}</span><div className={"warehouse-numbers "+(p.category==='เอ็นแบดมินตัน'?'':'warehouse-numbers-general')}><div><span>คงเหลือ</span><b>{p.stock}</b></div>{p.category==='เอ็นแบดมินตัน'&&<div><span>จองขึ้นเอ็น</span><b>{p.reserved}</b></div>}<div><span>พร้อมขาย</span><b className={p.stock-p.reserved<=0?'unpaid':''}>{p.stock-p.reserved}</b></div></div>{owner&&<div className="warehouse-cost"><span>ต้นทุน / {p.unit}</span><button className="text-button" onClick={()=>open('cost',{productId:p.id,cost:p.cost===null?'':p.cost/100})}>{p.cost===null?'รอกรอกต้นทุน':'฿'+fmt(p.cost)}</button></div>}{owner&&<footer>{p.active===0?<><button className="secondary" disabled={busy} onClick={()=>act('restoreProduct',{productId:p.id,requestId:crypto.randomUUID()})}>กู้คืนสินค้า</button><button className="secondary danger" disabled={busy} onClick={()=>{setDeleteTarget(p);setDeleteName('')}}>ลบสินค้าถาวร</button></>:<><button className="secondary" onClick={()=>open('editProduct',{productId:p.id,name:p.name,barcode:p.barcode,category:p.category,price:p.price/100,unit:p.unit,image:p.image,lowStock:p.low_stock})}>แก้ไข</button><button className="secondary" onClick={()=>open('adjustStock',{productId:p.id,qty:Math.max(0,p.stock),reason:'',name:p.name})}>ปรับยอด</button><button className="secondary danger" onClick={()=>setArchiveTarget(p)}>ลบสินค้า</button></>}</footer>}</article>)}</div>{!allProducts.some((p:any)=>(inventoryTab==='archived'?p.active===0:p.active!==0)&&((p.category==='เอ็นแบดมินตัน')===(inventoryKind==='strings')))&&<Empty text={inventoryTab==='archived'?'ไม่มีสินค้าที่ลบ':'ยังไม่มีสินค้าในคลัง'}/>}</>}
{inventoryTab==='receive'&&<div className="panel warehouse-receive"><h2>รับสินค้าเข้าคลัง</h2><p className="muted">เลือกสินค้าและระบุจำนวนที่รับเพิ่ม ระบบจะเพิ่มจากยอดคงเหลือเดิม</p><form onSubmit={async e=>{e.preventDefault();const d=await act('receive',{productId:receiveForm.productId,qty:receiveForm.qty,requestId:crypto.randomUUID()},false);if(d)setReceiveForm({productId:'',qty:1,query:''})}}><Field label="ค้นหา / สแกนสินค้า"><div className="search-box"><input placeholder="ชื่อสินค้าหรือบาร์โค้ด" value={receiveForm.query} onChange={e=>{const query=e.target.value,p=products.find((p:any)=>(p.barcode===query||p.scan_code===query));setReceiveForm({...receiveForm,query,productId:p?.id||''})}} onKeyDown={e=>{if(e.key==='Enter')e.preventDefault()}}/><button type="button" className="icon-button" aria-label="สแกนรับสินค้า" onClick={()=>setScanTarget('stockReceive')}><ScanBarcode size={22}/></button></div></Field><Field label="สินค้า"><Choice value={receiveForm.productId} onChange={(v:string)=>setReceiveForm({...receiveForm,productId:v})} options={products.filter((p:any)=>p.id===receiveForm.productId||`${p.name} ${p.barcode} ${p.scan_code||''}`.toLowerCase().includes(receiveForm.query.toLowerCase()))}/></Field><Field label="จำนวนที่รับเพิ่ม"><input required type="number" min="1" step="1" inputMode="numeric" value={receiveForm.qty} onChange={e=>setReceiveForm({...receiveForm,qty:e.target.value})}/></Field>{receiveForm.productId&&<div className="notice">คงเหลือ {products.find((p:any)=>p.id===receiveForm.productId)?.stock} → หลังรับเข้า {(products.find((p:any)=>p.id===receiveForm.productId)?.stock||0)+Number(receiveForm.qty||0)}</div>}<button disabled={busy||!receiveForm.productId}>{busy?'กำลังบันทึก…':'บันทึกการรับสินค้า'}</button></form></div>}
{inventoryTab==='history'&&<><div className="search-box"><Search size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="ค้นหาสินค้าในประวัติ"/></div><div className="stock-history">{(data.movements||[]).filter((m:any)=>{const p=allProducts.find((p:any)=>p.id===m.product_id);return `${p?.name||data.items.find((i:any)=>i.product_id===m.product_id)?.name||''} ${p?.barcode||''}`.toLowerCase().includes(search.toLowerCase())}).map((m:any)=><div className="stock-event" key={m.id}><span className="badge">{m.type}</span><div><b>{allProducts.find((p:any)=>p.id===m.product_id)?.name||data.items.find((i:any)=>i.product_id===m.product_id)?.name||'สินค้าที่ลบถาวร'}</b><small>{new Date(m.created).toLocaleString('th-TH',{timeZone:'Asia/Bangkok'})} · {name(m.staff_id)}</small>{m.reason&&<p>{m.reason}</p>}</div><strong className={m.delta>0?'paid':'unpaid'}>{m.delta>0?'+':''}{m.delta}</strong></div>)}</div>{!(data.movements||[]).length&&<Empty text="ยังไม่มีความเคลื่อนไหวสต๊อก"/>}</>}
</>}
{page==='stringing'&&<><div className="stats">{statuses.slice(0,4).map(s=><div key={s}><span>{s}</span><strong>{jobs.filter((j:any)=>j.status===s).length}<small> ไม้</small></strong></div>)}</div><div className="panel-tools"><div className="search-box"><Search size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="ชื่อลูกค้า / เบอร์โทร / รุ่นไม้"/></div><Choice value={jobFilter} onChange={setJobFilter} options={['ทั้งหมด',...statuses]}/></div><div className="job-grid">{jobs.filter((j:any)=>(jobFilter==='ทั้งหมด'||j.status===jobFilter)&&`${j.customer} ${j.phone} ${j.racket}`.toLowerCase().includes(search.toLowerCase())).map((j:any)=><button className="job-card" key={j.id} onClick={()=>showJob(j)}><div className="job-card-top"><span className={'badge '+(j.status==='พร้อมรับไม้'?'green':'')}>{j.status}</span><small>#{j.id.slice(0,8).toUpperCase()}</small></div><h3>{j.racket}</h3><p>{j.customer} · {j.phone}</p><div className="job-spec"><Scissors size={16}/>{products.find((p:any)=>p.id===j.product_id)?.name} · {j.tension}</div><footer><span className={j.paid?'paid':'unpaid'}>{j.paid?'ชำระแล้ว':'รอชำระ'} · ฿{fmt(j.amount)}</span><ChevronRight size={18}/></footer></button>)}</div>{!jobs.length&&<Empty text="ยังไม่มีงานขึ้นเอ็น" action={<button onClick={()=>open('job',{productId:'',stringerId:data.me.id,photos:[],condition:'ไม่มีตำหนิที่พบ',tension:'',amount:''})}>รับไม้ลูกค้า</button>}/>}</>}
{page==='dashboard'&&owner&&<>{dateControl}<div className="stats"><div><span>ยอดขายสุทธิ</span><strong>฿{fmt(revenue)}</strong><small>{sales.length} บิล</small></div><div><span>ต้นทุนสินค้า</span><strong>฿{fmt(cost)}</strong><small>{missing.length?'ยังไม่ครบ '+missing.length+' รายการ':'บันทึกต้นทุนครบ'}</small></div><div><span>ค่าแรง + คอมมิชชั่น</span><strong>฿{fmt(labor+commission)}</strong><small>ขึ้นเอ็น {fmt(labor)} · คอม {fmt(commission)}</small></div><div className="highlight"><span>กำไรหลังค่าใช้จ่าย</span><strong>{missing.length?'รอต้นทุน':'฿'+fmt(revenue-cost-labor-commission-expenses)}</strong><small>ค่าใช้จ่ายอื่น ฿{fmt(expenses)}</small></div></div>{missing.length>0&&<div className="notice">มีสินค้าที่ขายแล้วแต่ยังไม่มีต้นทุน {missing.length} รายการ กรอกที่หน้า “สินค้า / รับสินค้า” ก่อนสรุปกำไร</div>}<div className="report-grid"><div className="panel report"><h2>ยอดขายแยกหมวด</h2>{cats.slice(1).map(c=>{const amount=items.filter((x:any)=>x.category===c).reduce((s:number,x:any)=>s+x.net,0);return <div className="bar-row" key={c}><div><span>{c}</span><b>฿{fmt(amount)}</b></div><div className="bar-track"><div style={{width:(revenue?amount/revenue*100:0)+'%'}}/></div></div>})}</div><div className="panel report"><div className="flex justify-between items-center"><h2>ค่าใช้จ่ายอื่น</h2><button className="secondary small" onClick={()=>open('expense',{date:today()})}><Plus size={16}/>เพิ่ม</button></div><p className="muted">บันทึกค่าเช่า ค่าน้ำไฟ และค่าใช้จ่ายร้านเพื่อให้กำไรครบถ้วน</p>{data.expenses.filter((x:any)=>inRange(x.date+'T05:00:00Z')).map((x:any)=><div className="summary-row" key={x.id}><span>{x.name}<small>{x.date}</small></span><b>฿{fmt(x.amount)}</b></div>)}</div></div><h2 className="section-title">รายการขาย</h2><div className="panel"><Table><TableHeader><TableRow>{['เลขบิล / วันที่','พนักงาน','ชำระโดย','ส่วนลด','ยอดสุทธิ',''].map(x=><TableHead key={x}>{x}</TableHead>)}</TableRow></TableHeader><TableBody>{sales.map((s:any)=><TableRow key={s.id}><TableCell>#{s.id.slice(0,8).toUpperCase()}<small>{day(s.created)}</small></TableCell><TableCell>{name(s.staff_id)}</TableCell><TableCell>{s.method}</TableCell><TableCell>{fmt(s.discount)}</TableCell><TableCell><b>฿{fmt(s.total)}</b></TableCell><TableCell><button className="text-button" onClick={()=>open('receipt',s)}>ดูบิล</button></TableCell></TableRow>)}</TableBody></Table></div></>}
{page==='expenses'&&owner&&<>{dateControl}<div className="stats"><div><span>ค่าใช้จ่ายในช่วงที่เลือก</span><strong>฿{fmt(expenses)}</strong></div><div><span>จำนวนรายการ</span><strong>{data.expenses.filter((x:any)=>inRange(x.date+'T05:00:00Z')).length}</strong></div></div><div className="panel"><Table><TableHeader><TableRow><TableHead>วันที่</TableHead><TableHead>ประเภท</TableHead><TableHead>รายการค่าใช้จ่าย</TableHead><TableHead>จำนวนเงิน</TableHead><TableHead>รูปแนบ</TableHead></TableRow></TableHeader><TableBody>{data.expenses.filter((x:any)=>inRange(x.date+'T05:00:00Z')).map((x:any)=><TableRow key={x.id}><TableCell>{x.date}</TableCell><TableCell><span className="badge">{x.category}</span></TableCell><TableCell>{x.name}</TableCell><TableCell><b>฿{fmt(x.amount)}</b></TableCell><TableCell>{JSON.parse(x.photos||'[]').length?<button className="text-button" onClick={()=>open('expensePhotos',x)}>ดู {JSON.parse(x.photos).length} รูป</button>:'—'}</TableCell></TableRow>)}</TableBody></Table>{!data.expenses.some((x:any)=>inRange(x.date+'T05:00:00Z'))&&<Empty text="ยังไม่มีค่าใช้จ่ายในช่วงนี้" action={<button onClick={()=>open('expense',{date:today()})}><Plus size={18}/>บันทึกค่าใช้จ่าย</button>}/>}</div><p className="muted">ยอดที่บันทึกจะหักในรายงานกำไรตามวันที่รายการ ค่าแรงขึ้นเอ็นและคอมมิชชั่นระบบคำนวณแยกไว้แล้ว</p></>}
{page==='leave'&&<div className="panel"><Table><TableHeader><TableRow>{['พนักงาน','ประเภท','วันลา','เหตุผล','สถานะ','หมายเหตุ / จัดการ'].map(t=><TableHead key={t}>{t}</TableHead>)}</TableRow></TableHeader><TableBody>{data.leaves.map((l:any)=><TableRow key={l.id}><TableCell>{name(l.staff_id)}</TableCell><TableCell>{l.type}</TableCell><TableCell>{l.start} ถึง {l.end}</TableCell><TableCell>{l.reason}</TableCell><TableCell><span className="badge">{l.status}</span></TableCell><TableCell>{owner&&l.status==='รออนุมัติ'?<button className="secondary small" onClick={()=>open('leaveReview',{id:l.id,status:'อนุมัติ',note:''})}>พิจารณา</button>:l.note||'—'}</TableCell></TableRow>)}</TableBody></Table>{!data.leaves.length&&<Empty text="ยังไม่มีรายการลา"/>}</div>}
{page==='earnings'&&data.earnings&&<>{dateControl}<div className="notice">ค่าแรงนับวันที่สถานะเป็น “พร้อมรับไม้” · คอมมิชชั่นนับวันที่รับชำระหลังส่วนลด · แสดงยอดที่ได้รับสิทธิ์ ยังไม่ใช่หลักฐานจ่ายเงินเดือน</div><div className="panel"><Table><TableHeader><TableRow>{['พนักงาน','ขึ้นเอ็น (ไม้)','ค่าแรงขึ้นเอ็น','ยอดขายที่คิดคอม','คอมมิชชั่น 2%','รวม'].map(t=><TableHead key={t}>{t}</TableHead>)}</TableRow></TableHeader><TableBody>{members.filter((m:any)=>owner||m.id===data.me.id).map((m:any)=>{const count=data.earnings.jobs.filter((j:any)=>j.staff_id===m.id&&inRange(j.created)).length,ss=data.earnings.sales.filter((s:any)=>s.staff_id===m.id&&inRange(s.created)),amount=ss.reduce((v:number,x:any)=>v+x.net,0),comm=ss.reduce((v:number,x:any)=>v+Math.round(x.net*.02),0);return <TableRow key={m.id}><TableCell><b>{m.name}</b></TableCell><TableCell>{count}</TableCell><TableCell>฿{fmt(count*1500)}</TableCell><TableCell>฿{fmt(amount)}</TableCell><TableCell>฿{fmt(comm)}</TableCell><TableCell><b>฿{fmt(count*1500+comm)}</b></TableCell></TableRow>})}</TableBody></Table></div></>}
{page==='team'&&owner&&<><div className="stats"><div><span>ผู้ใช้ทั้งหมด</span><strong>{members.filter((m:any)=>m.email!=='sites-screenshot-service-noreply@chatgpt.com').length}</strong></div><div><span>เจ้าของร้านที่ใช้งาน</span><strong>{members.filter((m:any)=>m.active&&m.role==='owner').length}</strong></div><div><span>แคชเชียร์ที่ใช้งาน</span><strong>{members.filter((m:any)=>m.active&&m.role==='cashier').length}</strong></div></div><div className="panel"><Table><TableHeader><TableRow>{['ชื่อ','อีเมลเข้าสู่ระบบ','สิทธิ์','สถานะ',''].map(t=><TableHead key={t}>{t}</TableHead>)}</TableRow></TableHeader><TableBody>{members.filter((m:any)=>m.email!=='sites-screenshot-service-noreply@chatgpt.com').map((m:any)=><TableRow key={m.id}><TableCell><b>{m.name}</b></TableCell><TableCell>{m.email}</TableCell><TableCell>{m.role==='owner'?'เจ้าของกิจการ':'พนักงานแคชเชียร์'}</TableCell><TableCell>{m.active?'ใช้งาน':'ปิดบัญชี'}</TableCell><TableCell>{m.id!==data.me.id&&<div className="actions"><button className="text-button" onClick={()=>open('member',m)}>แก้ไข</button>{!!m.active&&<button className="text-button danger" disabled={busy} onClick={()=>act('deactivate',{id:m.id,requestId:crypto.randomUUID()})}>ปิดบัญชี</button>}</div>}</TableCell></TableRow>)}</TableBody></Table><div className="notice">พนักงานเข้าสู่ระบบด้วยบัญชี ChatGPT ที่ตรงกับอีเมลนี้ ต้องเปิดสิทธิ์เข้าถึงเว็บไซต์ให้พนักงานก่อนใช้งานร่วมกัน</div></div><h2 className="section-title">สิทธิ์แต่ละบทบาท</h2><div className="panel"><Table><TableHeader><TableRow><TableHead>การใช้งาน</TableHead><TableHead>เจ้าของร้าน</TableHead><TableHead>พนักงานแคชเชียร์</TableHead></TableRow></TableHeader><TableBody>{[['ขายสินค้า / ส่วนลด / แก้ราคาพร้อมหมายเหตุ','ได้','ได้'],['เพิ่มสินค้า / รับสินค้า / รับคืนไม้','ได้','ได้'],['ดูและกรอกต้นทุน / รายงานกำไร','ได้','ไม่ได้'],['เพิ่มผู้ใช้ / เปลี่ยนสิทธิ์ / ตั้งค่าร้าน','ได้','ไม่ได้'],['วันลา','ขอลาและอนุมัติ','ขอลาและดูของตนเอง'],['ค่าแรงและคอมมิชชั่น','ดูทุกคน / เปิด–ปิดการแสดง','ดูของตนเองเมื่อเปิดสิทธิ์']].map(([a,b,c])=><TableRow key={a}><TableCell>{a}</TableCell><TableCell>{b}</TableCell><TableCell>{c}</TableCell></TableRow>)}</TableBody></Table></div></>}
{page==='settings'&&owner&&<div className="settings-grid"><div className="panel report"><h2>ร้านค้าและสิทธิ์</h2><form onSubmit={e=>{e.preventDefault();act('settings',{shop:form.shop??data.config.shop,lineOa:form.lineOa??data.config.line_oa,showEarnings:form.showEarnings??!!data.config.show_earnings,requestId:crypto.randomUUID()},false)}}><Field label="ชื่อร้าน"><input value={form.shop??data.config.shop} onChange={e=>setForm({...form,shop:e.target.value})}/></Field><div className="switch-row"><div><b>แสดงค่าแรง / คอมมิชชั่นให้พนักงาน</b><p>เมื่อปิด พนักงานจะไม่เห็นหน้าและข้อมูลยอดนี้</p></div><Switch checked={form.showEarnings??!!data.config.show_earnings} onCheckedChange={v=>setForm({...form,showEarnings:v})}/></div><Field label="LINE OA ID (เช่น @yourshop)"><input value={form.lineOa??data.config.line_oa} onChange={e=>setForm({...form,lineOa:e.target.value})}/></Field><button disabled={busy}>บันทึกการตั้งค่า</button></form></div><div className="panel report"><h2>LINE และหน้าติดตามลูกค้า</h2><span className={'badge '+(data.lineReady?'green':'amber')}>{data.lineReady?'เชื่อมค่าระบบ LINE แล้ว':'ยังไม่เชื่อม LINE OA'}</span><p>ต้องตั้ง Channel access token และ Channel secret ของร้านก่อนส่งข้อความอัตโนมัติ</p><p>Webhook: <code>/api/line</code></p><p>ลูกค้าสแกน QR ใบรับไม้เพื่อติดตาม และส่งรหัส LINK ที่ได้รับให้ LINE OA เพื่อผูกการแจ้งเตือน</p><div className="notice">ขณะเว็บไซต์เป็นส่วนตัว ลูกค้าภายนอกและ LINE ยังเข้าไม่ได้ ต้องเปิดการเข้าถึงก่อนใช้งานจริง ข้อมูลหลังร้านยังตรวจสิทธิ์ทุกครั้ง</div></div><div className="panel report"><h2>หลักการคำนวณ</h2><p>ต้นทุนมาตรฐานต่อสินค้า: ต้นทุนที่กรอกจะเติมรายการรับเข้าและรายการขายที่ยังไม่มีต้นทุน ยอดที่มีต้นทุนแล้วเก็บค่าเดิมไว้</p><p>กำไร = ยอดขายหลังส่วนลด − ต้นทุนสินค้า − ค่าแรงขึ้นเอ็น − คอมมิชชั่น − ค่าใช้จ่ายที่บันทึก</p><p>วันที่รายงานใช้เวลาไทย รายงานนี้ยังไม่แยก VAT หรือภาษีเงินได้</p></div></div>}
</main><footer className="workspace-footer">WINGPRO POS <span>ข้อมูลร้านบันทึกออนไลน์</span></footer></div>
<Dialog open={!!modal} onOpenChange={v=>{if(!busy&&!uploading&&!v)setModal('')}}><DialogContent className={'pos-dialog '+(modal==='jobDetail'?'wide-dialog':'')}><DialogHeader><DialogTitle>{{quickProduct:'เพิ่มสินค้าจากบาร์โค้ด',editProduct:'แก้ไขสินค้า',adjustStock:'ปรับยอดสต๊อก',renameCategory:'แก้ชื่อหมวดหมู่',deleteCategory:'ลบหมวดหมู่',category:'สร้างหมวดหมู่สินค้า',product:'เพิ่มสินค้าใหม่',receive:'รับสินค้าเข้า',cost:'กรอกต้นทุนสินค้า',checkout:'รับชำระเงิน',job:'รับไม้ลูกค้า',jobDetail:'ใบรับไม้',payJob:'ชำระค่างานขึ้นเอ็น',leave:'ขอลา',leaveReview:'พิจารณาวันลา',member:'จัดการผู้ใช้และสิทธิ์',expense:'บันทึกค่าใช้จ่าย',expensePhotos:'รูปแนบค่าใช้จ่าย',receipt:'ใบเสร็จรับเงิน'}[modal]}</DialogTitle><DialogDescription>{modal==='cost'?'ใช้ต้นทุนมาตรฐาน เติมเฉพาะรายการเก่าที่ยังไม่มีต้นทุน':modal==='job'?'ตรวจสภาพไม้ร่วมกับลูกค้า และเก็บรูปก่อนขึ้นเอ็น':modal==='receive'?'พนักงานรับเข้าได้โดยไม่ต้องกรอกต้นทุน':modal==='jobDetail'?'เก็บ QR และเลขรับไม้ไว้ให้ลูกค้า':'กรอกข้อมูลและตรวจสอบก่อนบันทึก'}</DialogDescription></DialogHeader>
{modal==='jobDetail'&&selected?<div className="job-detail"><div><span className="badge">{selected.status}</span><h2>{selected.racket}</h2><p>{selected.customer} · {selected.phone}</p><p>เอ็น: {allProducts.find((p:any)=>p.id===selected.product_id)?.name||data.items.find((i:any)=>i.sale_id===data.sales.find((s:any)=>s.job_id===selected.id)?.id)?.name||'สินค้าที่ลบถาวร'}</p><p>ความตึง: {selected.tension}</p><p>ผู้ขึ้นเอ็น: {name(selected.stringer_id)}</p><Field label="สภาพไม้ก่อนขึ้นเอ็น"><p>{selected.condition}</p></Field><p>หมายเหตุ: {selected.note||'—'}</p><div className="photo-list">{JSON.parse(selected.photos).map((id:string)=><a key={id} href={'/api/files/'+id} target="_blank" rel="noreferrer"><img src={'/api/files/'+id} alt="สภาพไม้ก่อนขึ้นเอ็น"/></a>)}</div><h3>{selected.paid?'ชำระแล้ว':'รอชำระ'} ฿{fmt(selected.amount)}</h3><div className="actions wrap">{!selected.paid&&<button onClick={()=>open('payJob',{id:selected.id})}>รับชำระเงิน</button>}{!selected.returned&&<button disabled={busy||(selected.status==='พร้อมรับไม้'&&!selected.paid)} onClick={()=>act('jobStatus',{id:selected.id,status:statuses[statuses.indexOf(selected.status)+1],requestId:crypto.randomUUID()},false)}>เปลี่ยนเป็น {statuses[statuses.indexOf(selected.status)+1]}<ArrowRight size={16}/></button>}</div><p className="muted">LINE: {selected.notify}</p><button className="secondary small" disabled={busy||!data.lineReady} onClick={()=>act('notify',{id:selected.id,requestId:crypto.randomUUID()},false)}>ส่งแจ้งเตือนอีกครั้ง</button></div><div className="qr-panel">{qr&&<img src={qr} alt="QR ติดตามสถานะไม้"/>}<b>#{selected.id.slice(0,8).toUpperCase()}</b><a className="text-button" href={'/track/'+selected.token} target="_blank" rel="noreferrer">เปิดหน้าติดตาม ↗</a><button className="secondary small" onClick={()=>{navigator.clipboard.writeText(window.location.origin+'/track/'+selected.token).then(()=>toast.success('คัดลอกลิงก์แล้ว')).catch(()=>toast.error('คัดลอกไม่สำเร็จ'));}}>คัดลอกลิงก์</button><a className="text-button" download={'ticket-'+selected.id.slice(0,8)+'.png'} href={qr}>ดาวน์โหลด QR</a>{data.config.line_oa&&<a className="text-button" target="_blank" rel="noreferrer" href={'https://line.me/R/oaMessage/'+encodeURIComponent(data.config.line_oa)+'/?'+encodeURIComponent('LINK '+selected.token)}>ส่งรหัสผูก LINE</a>}<small>ลิงก์นี้เป็นส่วนตัวสำหรับลูกค้าเจ้าของไม้</small></div></div>:modal==='expensePhotos'?<div><h2>{form.name}</h2><div className="expense-gallery">{JSON.parse(form.photos||'[]').map((id:string,i:number)=><a key={id} href={'/api/files/'+id} target="_blank" rel="noreferrer"><img src={'/api/files/'+id} alt={'รูปแนบ '+(i+1)}/></a>)}</div></div>:modal==='receipt'?<div className="receipt-view"><h2>{data.config.shop}</h2><p>#{form.id?.slice(0,8).toUpperCase()} · {day(form.created)}</p>{data.items.filter((x:any)=>x.sale_id===form.id).map((x:any)=><div key={x.id}><div className="summary-row"><span>{x.name} × {x.qty}</span><b>{fmt(x.net)}</b></div>{x.line_discount>0&&<small>ส่วนลดรายการ {fmt(x.line_discount)} บาท</small>}{x.note&&<small>หมายเหตุราคา: {x.note}</small>}</div>)}<div className="grand-total"><span>ยอดสุทธิ</span><b>฿{fmt(form.total)}</b></div><p>{form.method} · ผู้ขาย {name(form.staff_id)}</p>{form.slip&&<a href={'/api/files/'+form.slip} target="_blank" rel="noreferrer">ดูสลิป</a>}</div>:<form onSubmit={async e=>{e.preventDefault();if(modal==='checkout'){const d=await act('sale',{items:cart.map(x=>({productId:x.id,qty:x.qty,price:x.salePrice,note:x.note,discount:x.discount||0,discountType:x.discountType||'baht'})),discount,discountType,method,slip:form.slip});if(d){setCart([]);setDiscount('0');setSaleScreen('catalog');open('receipt',{id:d.id,created:new Date().toISOString(),staff_id:data.me.id,total:d.total??total,method});}}else if(modal==='job'){const d=await act('job',form);if(d)toast.info('รับไม้แล้ว เปิดใบรับไม้เพื่อชำระตอนนี้ หรือชำระวันรับไม้');}else if(modal==='quickProduct'){if(!form.category){toast.error('กรุณาเลือกหมวดหมู่สินค้า');return;}const d=await act('product',form);if(d){add({...form,id:d.id,price:Math.round(Number(form.price)*100),stock:1,reserved:0});setSearch('');setSaleScreen('cart');}}else await act(modal,modal==='payJob'?{...form,method}:form);}}>
{modal==='quickProduct'&&<><p className="muted">ยังไม่พบสินค้าบาร์โค้ดนี้ กรอกข้อมูลเพื่อเพิ่มสินค้าและขายต่อ</p><Field label="บาร์โค้ดที่สแกน"><input readOnly value={form.barcode}/></Field><Field label="ชื่อสินค้า"><input autoFocus required maxLength={200} value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="ราคาขาย (บาท)"><input required type="number" inputMode="decimal" min="0" step="0.01" value={form.price} onChange={e=>setForm({...form,price:e.target.value})}/></Field><Field label="หมวดหมู่"><Choice value={form.category} onChange={(v:string)=>setForm({...form,category:v})} options={cats.slice(1)}/></Field><p className="muted">เจ้าของร้านแก้ไขหมวดหมู่ภายหลังได้ในคลังสินค้า</p><p className="muted">บันทึกสินค้าเข้าสต๊อก 1 ชิ้น และเพิ่มลงตะกร้า 1 ชิ้น ต้นทุนกรอกภายหลังได้</p></>}
{modal==='category'&&<><Field label="ชื่อหมวดหมู่ใหม่"><input required maxLength={60} value={form.name||''} onChange={e=>setForm({...form,name:e.target.value})} placeholder="เช่น ลูกแบดมินตัน / กริปพันด้าม"/></Field><p className="muted">หมวดหมู่ปัจจุบัน: {cats.slice(1).join(' · ')}</p><p className="muted">สินค้าที่เป็นเอ็นสำหรับขึ้นไม้ ให้ใช้หมวด “เอ็นแบดมินตัน” เพื่อไม่คิดคอมมิชชั่นและใช้ในงานขึ้นเอ็นได้</p></>}
{(modal==='product'||modal==='editProduct')&&<><Field label="ชื่อสินค้า"><input required value={form.name||''} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="บาร์โค้ด / รหัสสินค้า (เว้นว่างให้สร้างอัตโนมัติ)"><input value={form.barcode||''} onChange={e=>setForm({...form,barcode:e.target.value})} placeholder="wingpro-1, wingpro-2, … (สร้างอัตโนมัติ)"/><button type="button" className="secondary scan-field-button" onClick={()=>setScanTarget('product')}><ScanBarcode size={18}/>สแกนด้วยกล้อง</button></Field><Field label="หมวดสินค้า"><Choice value={form.category} onChange={(v:string)=>setForm({...form,category:v})} options={cats.slice(1)}/></Field><div className="form-grid"><Field label="ราคาขาย (บาท)"><input required type="number" min="0" step="0.01" value={form.price} onChange={e=>setForm({...form,price:e.target.value})}/></Field><Field label="หน่วย"><input required value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}/></Field></div>{modal==='product'&&<><Field label="จำนวนสินค้าเริ่มต้น"><input required type="number" inputMode="numeric" min="0" step="1" value={form.qty??1} onChange={e=>setForm({...form,qty:e.target.value})}/></Field><p className="muted">จำนวนนี้จะบันทึกเข้าสต๊อกและประวัติรับเข้าเมื่อเพิ่มสินค้า</p></>}{owner&&modal==='product'&&<Field label="ต้นทุนต่อหน่วย (บาท) — กรอกภายหลังได้"><input type="number" inputMode="decimal" min="0" step="0.01" value={form.cost??''} onChange={e=>setForm({...form,cost:e.target.value})} placeholder="ยังไม่ทราบต้นทุน ให้เว้นว่าง"/></Field>}<Field label="แจ้งเตือนเมื่อพร้อมขายน้อยกว่า (ชิ้น)"><input type="number" min="0" step="1" value={form.lowStock??5} onChange={e=>setForm({...form,lowStock:e.target.value})}/></Field><Field label="รูปสินค้า (JPG / PNG / WebP ไม่เกิน 8 MB)"><input type="file" accept="image/jpeg,image/png,image/webp" disabled={uploading} onChange={e=>upload(e.target.files,'image')}/></Field>{form.image&&<div className="product-upload-preview"><img src={'/api/files/'+form.image} alt="รูปสินค้า"/><button type="button" className="secondary" disabled={uploading} onClick={()=>setForm({...form,image:null})}>ลบรูป</button></div>}<p className="muted">เอ็นให้นับเป็นชุดต่อไม้ หากซื้อเป็นม้วน ให้แปลงเป็นจำนวนชุดที่ใช้ได้ก่อนรับเข้า</p></>}
{modal==='receive'&&<><Field label="ค้นหาหรือสแกนสินค้า"><input placeholder="ชื่อสินค้า / บาร์โค้ด" value={form.query||''} onChange={e=>{const query=e.target.value,p=products.find((p:any)=>(p.barcode===query||p.scan_code===query));setForm({...form,query,...p?{productId:p.id}:{}})}} onKeyDown={e=>{if(e.key==='Enter')e.preventDefault()}}/><button type="button" className="secondary scan-field-button" onClick={()=>setScanTarget('receive')}><ScanBarcode size={18}/>สแกนด้วยกล้อง</button></Field><Field label="สินค้า"><Choice value={form.productId} onChange={(v:string)=>setForm({...form,productId:v})} options={products.filter((p:any)=>p.id===form.productId||`${p.name} ${p.barcode} ${p.scan_code||''}`.toLowerCase().includes((form.query||'').toLowerCase()))}/></Field><Field label="จำนวนรับเข้า"><input type="number" min="1" step="1" required value={form.qty} onChange={e=>setForm({...form,qty:e.target.value})}/></Field></>}
{modal==='adjustStock'&&<><p>{form.name}</p><Field label="จำนวนคงเหลือที่ตรวจนับได้"><input required type="number" min="0" step="1" value={form.qty} onChange={e=>setForm({...form,qty:e.target.value})}/></Field><Field label="เหตุผลที่ปรับยอด"><textarea required value={form.reason} onChange={e=>setForm({...form,reason:e.target.value})}/></Field></>}{modal==='renameCategory'&&<Field label="ชื่อใหม่"><input required value={form.newName} onChange={e=>setForm({...form,newName:e.target.value})}/></Field>}{modal==='deleteCategory'&&<p>ยืนยันลบหมวด “{form.name}” ต้องย้ายสินค้าในหมวดนี้ออกก่อน รวมสินค้าที่ลบไว้</p>}{modal==='cost'&&<Field label="ต้นทุนมาตรฐานต่อหน่วย (บาท)"><input type="number" min="0" step="0.01" required value={form.cost} onChange={e=>setForm({...form,cost:e.target.value})}/></Field>}
{(modal==='checkout'||modal==='payJob')&&<><div className="payment-total">฿{fmt(modal==='checkout'?total:selected?.amount||0)}</div><Field label="วิธีชำระเงิน"><Choice value={method} onChange={setMethod} options={['เงินสด','โอนเงิน','บัตร']}/></Field><Field label="แนบสลิป (ถ้ามี)"><input type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>upload(e.target.files,'slip')}/></Field>{form.slip&&<p className="paid">แนบสลิปแล้ว ✓</p>}<p className="muted">ตรวจสอบว่าได้รับเงินจริงก่อนยืนยัน สลิปที่แนบไม่ได้ตรวจยอดโอนอัตโนมัติ</p></>}
{modal==='job'&&<><div className="form-grid"><Field label="ชื่อลูกค้า"><input required value={form.customer||''} onChange={e=>setForm({...form,customer:e.target.value})}/></Field><Field label="เบอร์โทร"><input required type="tel" value={form.phone||''} onChange={e=>setForm({...form,phone:e.target.value})}/></Field></div><Field label="ยี่ห้อ / รุ่นไม้"><input required value={form.racket||''} onChange={e=>setForm({...form,racket:e.target.value})}/></Field><Field label="เอ็น"><Choice value={form.productId} onChange={(v:string)=>setForm({...form,productId:v,amount:(products.find((p:any)=>p.id===v)?.price||0)/100})} options={products.filter((p:any)=>p.category==='เอ็นแบดมินตัน')}/></Field><div className="form-grid"><Field label="ความตึง (เช่น 25 lbs)"><input required value={form.tension} onChange={e=>setForm({...form,tension:e.target.value})}/></Field><Field label="ยอดชำระรวม (บาท)"><input required type="number" min="0" step="0.01" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/></Field></div><Field label="พนักงานขึ้นเอ็น"><Choice value={form.stringerId} onChange={(v:string)=>setForm({...form,stringerId:v})} options={members.filter((m:any)=>m.active)}/></Field><Field label="สภาพไม้ / จุดตำหนิ"><textarea required value={form.condition} onChange={e=>setForm({...form,condition:e.target.value})}/></Field><Field label="รูปสภาพไม้ก่อนขึ้น (สูงสุด 8 รูป รูปละ 8 MB)"><input type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={e=>upload(e.target.files,'photos')}/></Field><p className="muted">แนบแล้ว {(form.photos||[]).length} รูป</p><Field label="หมายเหตุ"><textarea value={form.note||''} onChange={e=>setForm({...form,note:e.target.value})}/></Field><p className="muted">บันทึกแล้วเอ็นจะถูกจอง เปิดใบรับไม้เพื่อรับชำระทันที หรือรับชำระวันที่ลูกค้ามารับ</p></>}
{modal==='leave'&&<><Field label="ประเภทวันลา"><Choice value={form.type} onChange={(v:string)=>setForm({...form,type:v})} options={['ลากิจ','ลาป่วย','ลาพักร้อน','อื่น ๆ']}/></Field><div className="form-grid"><Field label="ตั้งแต่"><input required type="date" value={form.start} onChange={e=>setForm({...form,start:e.target.value})}/></Field><Field label="ถึง"><input required type="date" min={form.start} value={form.end} onChange={e=>setForm({...form,end:e.target.value})}/></Field></div><Field label="เหตุผล"><textarea required value={form.reason||''} onChange={e=>setForm({...form,reason:e.target.value})}/></Field></>}
{modal==='leaveReview'&&<><Field label="ผลพิจารณา"><Choice value={form.status} onChange={(v:string)=>setForm({...form,status:v})} options={['อนุมัติ','ไม่อนุมัติ']}/></Field><Field label="หมายเหตุ"><textarea value={form.note} onChange={e=>setForm({...form,note:e.target.value})}/></Field></>}
{modal==='member'&&<><Field label="ชื่อผู้ใช้"><input required value={form.name||''} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="อีเมลบัญชี ChatGPT"><input required type="email" disabled={!!form.id} value={form.email||''} onChange={e=>setForm({...form,email:e.target.value})}/></Field><Field label="สิทธิ์"><Choice value={form.role} onChange={(v:string)=>setForm({...form,role:v})} options={[{id:'cashier',name:'พนักงานแคชเชียร์'},{id:'owner',name:'เจ้าของร้าน'}]}/></Field></>}
{modal==='expense'&&<><Field label="วันที่ค่าใช้จ่าย"><input required type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})}/></Field><datalist id="expense-types">{['ค่าเช่า','ค่าน้ำ / ค่าไฟ','ค่าจ้าง','ค่าขนส่ง','การตลาด','ซ่อมแซม','วัสดุสิ้นเปลือง','อื่น ๆ'].map(t=><option value={t} key={t}/>)}</datalist><div className="expense-entry-list">{(form.items||[]).map((row:any,i:number)=><div className="expense-entry" key={i}><div className="flex items-center justify-between"><b>รายการที่ {i+1}</b><button type="button" className="icon-button" aria-label={'ลบรายการที่ '+(i+1)} disabled={uploading||form.items.length===1} onClick={()=>setForm({...form,items:form.items.filter((_:any,k:number)=>k!==i)})}><Trash2 size={17}/></button></div><Field label="ประเภทรายจ่าย"><input required maxLength={60} list="expense-types" placeholder="เลือกหรือพิมพ์ประเภทเอง" value={row.category} onChange={e=>setForm({...form,items:form.items.map((x:any,k:number)=>k===i?{...x,category:e.target.value}:x)})}/></Field><Field label="รายละเอียด"><input required value={row.name} placeholder="เช่น ค่าไฟร้าน เดือนกันยายน" onChange={e=>setForm({...form,items:form.items.map((x:any,k:number)=>k===i?{...x,name:e.target.value}:x)})}/></Field><Field label="จำนวนเงิน (บาท)"><input required min="0.01" type="number" inputMode="decimal" step="0.01" value={row.amount} onChange={e=>setForm({...form,items:form.items.map((x:any,k:number)=>k===i?{...x,amount:e.target.value}:x)})}/></Field><Field label="รูปใบเสร็จ / หลักฐาน (สูงสุด 8 รูปต่อรายการ)"><input type="file" accept="image/jpeg,image/png,image/webp" multiple disabled={uploading} onChange={e=>{uploadExpense(e.target.files,i);e.target.value=''}}/></Field><div className="expense-photo-thumbs">{(row.photos||[]).map((id:string)=><div key={id}><a href={'/api/files/'+id} target="_blank" rel="noreferrer"><img src={'/api/files/'+id} alt="รูปแนบค่าใช้จ่าย"/></a><button type="button" className="icon-button" aria-label="ลบรูปแนบ" disabled={uploading} onClick={()=>setForm({...form,items:form.items.map((x:any,k:number)=>k===i?{...x,photos:x.photos.filter((p:string)=>p!==id)}:x)})}><Trash2 size={15}/></button></div>)}</div></div>)}</div><button className="secondary" type="button" disabled={(form.items||[]).length>=50} onClick={()=>setForm({...form,items:[...form.items,{category:'',name:'',amount:''}]})}><Plus size={18}/>เพิ่มรายการ</button><div className="grand-total"><span>รวม {(form.items||[]).length} รายการ</span><strong>฿{fmt((form.items||[]).reduce((sum:number,x:any)=>sum+Math.round((Number(x.amount)||0)*100),0))}</strong></div></>}

<div className="dialog-actions"><button type="button" className="secondary" disabled={busy||uploading} onClick={()=>setModal('')}>ยกเลิก</button><button disabled={busy||uploading}>{uploading?'กำลังแนบรูป…':busy?'กำลังบันทึก…':modal==='quickProduct'?'บันทึกและเพิ่มลงตะกร้า':modal==='checkout'||modal==='payJob'?'ยืนยันรับชำระเงิน':'บันทึก'}</button></div></form>}
</DialogContent></Dialog><AlertDialog open={!!archiveTarget} onOpenChange={v=>{if(!v)setArchiveTarget(null)}}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>ลบสินค้า {archiveTarget?.name}?</AlertDialogTitle><AlertDialogDescription>สินค้าจะย้ายไป “สินค้าที่ลบ” และไม่แสดงให้ขายหรือรับเข้า ประวัติและยอดคงเหลือยังเก็บไว้ สามารถกู้คืนได้</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>ยกเลิก</AlertDialogCancel><AlertDialogAction disabled={busy} onClick={async()=>{const p=archiveTarget;const d=await act('archiveProduct',{productId:p.id,requestId:crypto.randomUUID()});if(d){setArchiveTarget(null);setCart(c=>c.filter(x=>x.id!==p.id))}}}>ยืนยันลบสินค้า</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><AlertDialog open={!!deleteTarget} onOpenChange={v=>{if(!v&&!busy)setDeleteTarget(null)}}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>ลบสินค้าถาวร?</AlertDialogTitle><AlertDialogDescription>สินค้า {deleteTarget?.name} พร้อมยอดคงเหลือ ประวัติรับเข้าและปรับยอดจะถูกลบออกจากระบบและกู้คืนไม่ได้ บิลขายและประวัติงานขึ้นเอ็นเดิมยังเก็บไว้</AlertDialogDescription></AlertDialogHeader><Field label="พิมพ์ชื่อสินค้าเพื่อยืนยัน"><input value={deleteName} disabled={busy} onChange={e=>setDeleteName(e.target.value)} autoComplete="off"/></Field><AlertDialogFooter><AlertDialogCancel disabled={busy}>ยกเลิก</AlertDialogCancel><AlertDialogAction className="danger" disabled={busy||deleteName!==deleteTarget?.name} onClick={async e=>{e.preventDefault();if(busy||!deleteTarget)return;const p=deleteTarget;const d=await act('deleteProductPermanently',{productId:p.id,confirmName:deleteName,requestId:crypto.randomUUID()},false);if(d){setDeleteTarget(null);setCart(c=>c.filter(x=>x.id!==p.id))}}}>{busy?'กำลังลบ…':'ยืนยันลบถาวร'}</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><BarcodeScanner open={!!scanTarget} onClose={()=>setScanTarget('')} onDetected={detected}/></SidebarProvider>}
