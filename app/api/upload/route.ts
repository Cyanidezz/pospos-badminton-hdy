import {auth,saveUploadedFile} from '@/lib/server';
export async function POST(req:Request){try{const me=await auth();if(req.headers.get('origin')&&req.headers.get('origin')!==new URL(req.url).origin)throw new Error('คำขอไม่ถูกต้อง');const form=await req.formData(),file=form.get('file');if(!(file instanceof File))throw new Error('ไม่พบไฟล์');const {id}=await saveUploadedFile(file,me.id);return Response.json({id});}catch(e:any){return Response.json({error:e.message},{status:400});}}
