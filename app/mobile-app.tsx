'use client';
import {useEffect,useState} from 'react';
import {Download,WifiOff,X} from 'lucide-react';
type InstallEvent=Event & {prompt:()=>Promise<void>;userChoice:Promise<{outcome:string}>};
export default function MobileApp(){const [install,setInstall]=useState<InstallEvent|null>(null),[standalone,setStandalone]=useState(true),[online,setOnline]=useState(true),[help,setHelp]=useState(false),[dismissed,setDismissed]=useState(false);
useEffect(()=>{setStandalone(window.matchMedia('(display-mode: standalone)').matches||!!(navigator as Navigator&{standalone?:boolean}).standalone);setOnline(navigator.onLine);const off=()=>setOnline(false),on=()=>setOnline(true),ready=(e:Event)=>{e.preventDefault();setInstall(e as InstallEvent)},installed=()=>{setStandalone(true);setInstall(null)};window.addEventListener('offline',off);window.addEventListener('online',on);window.addEventListener('beforeinstallprompt',ready);window.addEventListener('appinstalled',installed);if('serviceWorker' in navigator)navigator.serviceWorker.register('/sw.js',{scope:'/'}).catch(()=>{});return()=>{window.removeEventListener('offline',off);window.removeEventListener('online',on);window.removeEventListener('beforeinstallprompt',ready);window.removeEventListener('appinstalled',installed)}},[]);
return <>{!online&&<div role="status" className="connection-banner"><WifiOff size={18}/>ไม่มีอินเทอร์เน็ต กรุณาเชื่อมต่อก่อนขายหรือบันทึกข้อมูล</div>}{!standalone&&!dismissed&&<div className="install-banner"><div><strong>ติดตั้ง Wingpro POS</strong><span>เปิดร้านจากหน้าจอโฮม</span></div><button onClick={async()=>{if(install){await install.prompt();await install.userChoice;setInstall(null)}else setHelp(!help)}}><Download size={16}/><span>ติดตั้งแอป</span></button><button className="icon-button" aria-label="ปิดคำแนะนำติดตั้ง" onClick={()=>setDismissed(true)}><X size={18}/></button>{help&&<p>iPhone / iPad: เปิดใน Safari → แชร์ → เพิ่มไปยังหน้าจอโฮม → เปิด “เปิดเป็นเว็บแอป” ถ้ามี → เพิ่ม<br/>Android: เปิดใน Chrome → เมนู ⋮ → ติดตั้งแอป หรือเพิ่มไปยังหน้าจอหลัก</p>}</div>}</>;
}
