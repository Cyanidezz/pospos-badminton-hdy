// Cache only the generic offline document, never shop records or authenticated pages.
const CACHE='badminton-offline-v2';
self.addEventListener('install',event=>{event.waitUntil((async()=>{const r=await fetch('/offline.html',{cache:'reload'});if(r.ok&&(await r.clone().text()).includes('badminton-offline-document')){const c=await caches.open(CACHE);await c.put('/offline.html',r)}await self.skipWaiting()})())});
self.addEventListener('activate',event=>{event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith('badminton-offline-')&&key!==CACHE)await caches.delete(key);await self.clients.claim()})())});
self.addEventListener('fetch',event=>{const url=new URL(event.request.url);if(event.request.method!=='GET'||url.origin!==self.location.origin||event.request.mode!=='navigate'||url.pathname.startsWith('/api/')||url.pathname.includes('signin-with-chatgpt')||url.pathname.includes('signout-with-chatgpt')||url.pathname==='/callback')return;event.respondWith(fetch(event.request).catch(async()=>await caches.match('/offline.html')||new Response('ไม่มีอินเทอร์เน็ต กรุณาเชื่อมต่อและลองอีกครั้ง',{status:503,headers:{'Content-Type':'text/plain;charset=utf-8'}})))});
